// Single Quote string
let carName = 'Benz';
console.log(carName);

//Double Quote string
let bikeName = "Royal Enfield";
console.log(bikeName);

//Javascript specifies that both single and double quotes can be used to define string literals. The choice between them is often a matter of style and convenience. For example, if you want to include a single quote within a string, it's easier to use double quotes to define the string, and vice versa.
//Javascript prefer to use single quotes for string literals, but it is not a strict rule. The choice between single and double quotes is often a matter of style and consistency within a codebase. Some developers prefer to use single quotes for strings that do not contain any single quotes, and double quotes for strings that contain single quotes, to avoid the need for escaping characters. Ultimately, the most important thing is to be consistent in your choice of quotes throughout your code.